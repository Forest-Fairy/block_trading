# 进度记录

## 2026-08-12
- 创建帖子领域建模与实体设计计划，开始定位产品规则、原型数据和发布表单。
- 已完成产品文档、业务模块规则和需求基线阅读；开始对照原型数据、详情展示与发布 Sheet 进行字段审计。
- 已审计原型帖子数据和悬浮助手草稿模型，确认当前为展示字符串模型，无法直接作为领域实体或发布校验模型使用。
- 已完成定位阶段；确定采用“帖子主表 + 地点/媒体/参与关系 + 五类扩展表”的关系模型，避免单表膨胀和 JSON 字段失去查询能力。
- 根据用户反馈，MySQL DDL 将替换为 Oracle 逻辑字段、长度/精度和关系设计。
- 已完成五类帖子字段矩阵、公共/扩展实体模型与 Oracle 逻辑实体设计；开始以共享字段模式改造发布输入和详情展示。
- 已新增 `post-schema.ts`，让社区发布 Sheet 与悬浮助手手动建帖按五类帖子切换必填和可选字段。
- 已为原型帖子补充结构化类型详情，详情页改为读取 `getActivityDetailFields`；产品、业务模块和 Oracle 实体文档同步更新。
- 已修正助手建帖头部在 390×844 移动端的按钮与标题挤压问题。
- `npm run lint`、`npm run build` 通过；浏览器复核了线上开黑详情、拼单发布表单和无控制台错误。
- 已完成跨模块实体清单：账户身份、校园/会员、关系位置、社区帖子、消息群聊、商城交易、推荐运营七组，共 56 张逻辑表。
- 已生成 `block_trading_docs/数据库设计/趣汇核心实体设计.md` 与 `趣汇_oracle_schema.sql`；脚本仅含建表、唯一/检查约束和索引，不含 SQL 外键。
- 系统设计校验补齐登录设备、收藏、邀请、兑换码、订单配送、商品评价、客服工单及用户隐私/免打扰字段；支付/参与/活动、订单/支付/履约、通知/会话/消息状态保持独立。
- 追加消息媒体与配送轨迹实体，并形成“系统功能 -> 实体 -> 校验结论”矩阵；确认文档已覆盖的首期功能均有持久化承载，跨表状态机和权限留在应用服务层。
- 根据系统级产品规划补齐用户转卖最小模型：刊登、媒体与订单项来源；未扩展尚未定义流程的议价、担保与票券核销。
- 最终静态验证：56 张表、56 个主键、19 个索引、39 个检查约束；无重复表/约束名、无超出 Oracle 30 字符的约束名、无 `FOREIGN KEY`/`REFERENCES`、括号平衡。根 Maven `validate` 通过。
