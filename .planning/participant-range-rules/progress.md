# 执行记录

- 已读取项目规则、PROJECT.md、现有命名统一计划及人数相关代码/文档片段。
- 已确认本轮范围为拼车与线上开黑的最少/最多人数，不扩展拼单和线下组队的既有目标人数模型。
- 已将活动数据改为当前人数、最少人数、最多人数；卡片和搜索结果展示人数范围，发布 Sheet 对拼车/线上开黑展示独立上下限输入。
- 已同步项目产品定义、推荐/社区产品文档、推荐/社区业务文档和首期需求文档。
- 已完成 `npm run lint`、`npm run build`、`git diff --check`；源码已无旧 `people` 字段引用。
- 页面复核沿用已启动的 `http://127.0.0.1:4174/`，若浏览器运行时无法访问发布 Sheet，将记录为环境限制而不绕过安全策略。
- 已确认 `4174` 端口开发服务已启动，首页截图显示新分类入口。
- 已在社区发布 Sheet 中分别切换拼车、线上开黑，截图确认两类均使用最少/最多人数输入。
