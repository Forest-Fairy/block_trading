# 现状发现

- 活动数据没有作者、关注关系、校区或可见性字段，推荐、社区和搜索直接遍历同一 `activities` 数组。
- ProfilePage 已有偏好设置区域，但项目中没有现成 Switch 组件，需要用语义化 checkbox 开关实现。
- App 已集中维护全局页面状态，校园版应提升到 App 再传入推荐、社区、搜索与个人中心。
- 当前 ProfilePage 不展示用户编号，可保持该规则不回退。
- 浏览器复核：原型在 `http://127.0.0.1:4174/` 正常加载；“我的”页面可见“学生认证”、禁用的“校园版”开关、仅自身可见说明和“去认证”入口。
- 浏览器复核：认证弹窗包含学校和学生证号字段，完成后显示“杭州大学 · 已认证”和“仅自己可见”，校园版开关变为可用。
- 用户要求学生认证位于“我的”页面底部区域，并进一步明确退出登录必须是最后一项；原型最终顺序为偏好设置、学生认证与校园版、退出登录。
- 浏览器复核首轮滚动参数使用了 `deltaY`，而当前 CUA 要求 `scrollX/scrollY`；改用正确参数继续验证，不重复原调用。
- 浏览器阶段复核：认证区域滚动后可完整查看，固定底部导航未遮挡内容；最终顺序根据最新反馈调整为认证区域之后显示退出登录。
