# 进度记录

## 2026-08-13

- 已创建迭代生命周期测试用例扩充任务并切换活动计划。
- 已对照产品分期、架构验收、领域边界和现有 25 条用例，完成覆盖缺口分类。
- 已在功能测试用例文档新增周期测试执行规则，并按主业务闭环、权限与人工收口、异步/外部失败、发布与回退补充 39 条用例。
- 扩充后共 64 条用例：R1 26 条、R2 15 条、R3 11 条、R4 12 条；所有用例 ID 唯一，引用的测试模块均已在代码模块规划中声明。
- 已验证本地 Markdown 链接、Maven Reactor 和 diff 空白；`mvn -q -DskipTests validate` 通过。当前无业务代码模块，文档用例尚未转化为可运行测试。
- 已修正 Maven Reactor 图：删除混入聚合图的运行时和跨层依赖箭头，使每个模块只显示一个聚合父节点；运行时装配与编译期依赖继续由后续文字规则表达。
- 已将部署交付从 Runtime 分离为 `quhui_deployment`：Runtime 只产出可执行 JAR，部署模块按单元管理镜像、环境 overlay、密钥引用、发布与回滚脚本，并按 R1-R4 增量扩展。
