# 执行记录

- 已定位状态栏、底部导航、社区标题、列表搜索与校园模式状态流。
- 已确定顶部按钮遵守学生认证前置条件，校园模式下统一替换社区显示文案。
- 已实现顶部状态栏校园版按钮、底部导航动态标签和社区页动态标题。
- 已移除社区列表搜索框、查询状态和查询过滤，保留右上角全局搜索入口。
- 已同步产品社区、移动端交互规范和业务社区文档，并修正模块化后的源码路径。
- `npm run lint`、`npm run typecheck`、`npm run build`、`git diff --check` 均通过；已复核普通模式、认证后校园模式、社区无列表搜索框和“校园”动态文案。
- 根据反馈，顶部按钮改为原型预览快捷入口：开启时同步模拟学生认证，不再禁用；社区活动相关文案同步动态显示。
- `npm run lint`、`npm run typecheck`、`npm run build`、`git diff --check` 均通过；浏览器复核顶部按钮直开预览、绿色“校园中”、社区标题“校园”、底部入口“校园”和无列表搜索框。
