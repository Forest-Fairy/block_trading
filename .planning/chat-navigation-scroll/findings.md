# 发现记录

- 用户消息聊天承载在 `MessagesPage.tsx` 的 `ChatView`，首条群聊提示使用当前主题色文本。
- 页面路由与底部导航集中于 `App.tsx`，适合统一处理导航可见性、双击和页面滚动位置。
- 各业务页本身未统一接收滚动容器引用，需要确认原型的实际滚动根节点后实现恢复。
- 固定布局尺寸主要位于手机壳、悬浮助手、列表媒体、营销卡片和小程序快捷入口；这些元素可分别改用视口约束、`clamp()`、`aspect-ratio` 和容器百分比。
- 1px 描边、阴影扩散和图标视觉尺寸属于可保留的绝对值例外，不承担页面布局职责。
