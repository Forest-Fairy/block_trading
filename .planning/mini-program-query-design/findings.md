# 现状发现

- ProfilePage 当前每次渲染都在组件内创建静态 `miniPrograms` 数组，没有性别适用、收藏、排序值、使用频次、流量热度或查询时间。
- “探索更多”是纯文本，没有右箭头。
- 系统设置标题仍为“系统设置”；页面没有发现用户编号字段，需要保持不新增该展示。
- 现有 `useEffect`、`useRef`、`useState` 足以实现一分钟缓存与定时刷新，不需要新的依赖。
- 页面截图确认最近查询时间与“当前性别适用”提示正常显示；排序结果为收藏项优先，显式排序为空的“周末组局”排在列表末尾并通过横向露出提示还有内容。
- “探索更多”右侧箭头清晰，动态四项使用横向滚动，没有产生孤立换行或挤压兑换码区域。
- 下滑截图确认标题已改为“偏好设置”，右侧没有用户编号或其他辅助字段；页面运行控制台无错误。
