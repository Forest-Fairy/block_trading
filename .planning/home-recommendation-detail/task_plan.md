# 首页推荐页详细规划与原型

## 目标
基于现有趣汇产品文档和 shadcn-mobile 原型，完成首页推荐页的详细产品规划、交互说明、可运行原型与截图复核。

## 阶段
- [complete] 1. 现状调研与方案定稿
- [complete] 2. 产品文档与业务文档回写
- [complete] 3. 推荐页原型实现
- [complete] 4. 构建、运行与截图复核
- [complete] 5. 交付整理

## 决策
- 原型入口：`block_trading_docs/产品原型/shadcn-mobile`
- 首屏采用可解释混合信息流，保留当前五入口导航。
- 首期展示真实业务状态的静态样例，交互使用本地状态模拟。

## 第二轮改进
- [complete] 1. 搜索按钮改为独立搜索页
- [complete] 2. 通知图标补充未读数量
- [complete] 3. 订单状态改为紧凑三行轮播
- [complete] 4. 首页分类改为旅游与游戏并完善各分类列表头
- [complete] 5. 今日重点改为左上角“今日首推”并显示距离
- [complete] 6. 文档、构建、交互与截图复核

## 第三轮改进
- [complete] 1. 订单状态标题与间距收紧，查看全部字号下调
- [complete] 2. 订单状态按组三条慢速循环滚动
- [complete] 3. 关注分类标题与排序恢复为“为您推荐”
- [complete] 4. 内容列表移除活动提醒
- [complete] 5. 文档、构建、交互与截图复核

## 第四轮改进
- [complete] 1. 订单状态支持触摸、滚轮和键盘手动滚动
- [complete] 2. 手动操作与三条成组自动循环建立暂停和恢复规则
- [complete] 3. 产品规划与业务文档集中记录全部设计约束
- [complete] 4. 构建、手动交互、自动循环与截图复核

## 第五轮命名统一
- [complete] 1. 首页娱乐分类统一为线下组队与线上开黑
- [complete] 2. 社区 Tabs、发布类型与活动数据统一使用相同分类值
- [complete] 3. 产品文档、业务文档和导航摘要同步命名基线
- [complete] 4. 构建、关键词检查与原型交互复核

## 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| `request_user_input` 在 Default 模式不可用 | 1 | 按项目规则直接采用现有 shadcn-mobile 工作区 |
| 本地开发服务首次后台启动被系统拒绝 | 2 | 使用受控长运行会话启动 Vite 并确认 4173 端口监听 |
| 最终截图时旧服务退出导致连接拒绝 | 1 | 重启服务、重新加载页面并覆盖错误截图 |
| 清理本轮 `dist/` 和 `vite.log` 被策略拦截 | 1 | 不再尝试破坏性清理，保留为未跟踪构建产物 |
| 第二轮首次构建发现 `Compass`、`X` 未使用 | 1 | 删除搜索改造后遗留的图标导入，lint 与构建通过 |
| 订单状态卡片初次截图高度为 180px | 1 | 收紧标题、行高、图标和辅助文字，复核高度为 158px |
| PowerShell 端口监听探测无输出并返回状态 1 | 1 | 改用直接 HTTP 请求探测服务，必要时启用备用端口 |
| `npm run dev -- --host 127.0.0.1 --port 4174` 被 Vite 8 解析为多余位置参数 | 1 | 改用 `--host=...` 与 `--port=...` 等号形式启动 |
| `Start-Process` 后台启动被当前执行策略拦截 | 1 | 改用无窗口的 `cmd /c start /b` 启动本地 Vite 进程 |
| `cmd /c start /b` 未能保活服务且调用超时 | 1 | 改为受控长运行会话前台启动，验收后显式终止 |
| 浏览器控制层不支持 `networkidle` 等待状态 | 1 | 复用已打开标签，改用 `domcontentloaded` 与短暂稳定等待 |
| 独立 Node 启动包装脚本被 PowerShell 剥离引号而语法错误 | 1 | HTTP 探测确认原 Vite 子进程仍在 4174 正常服务，不再重复启动 |
| 手动滚动首个整块补丁因 Prettier 上下文变化无法匹配 | 1 | 未产生代码写入，改为读取精确片段并拆分小补丁 |
| 自动恢复修正后 lint 提示 effect 清理读取可变 ref | 1 | 在 effect 初始化时捕获稳定容器引用供定时器与清理函数共用 |
| 最终复测的第二次坐标滚轮注入在浏览器控制层超时 | 1 | 页面无错误；保留此前真实滚轮结果，改用同事件仲裁路径的键盘输入完成最终暂停与恢复复测 |
