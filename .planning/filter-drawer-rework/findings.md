# 现状发现

- 原型首页分类条仍包含“附近”，分类状态和内容类型筛选混在同一组按钮中。
- 社区页已有筛选图标但未形成按业务分类变化的侧边筛选字段。
- 商城页已有搜索和排序入口，缺少电商式筛选抽屉。
- 活动筛选可复用 Sheet/Drawer 组件和已有 `createType` 状态，避免新增页面级路由。
- 最新首页截图确认分类条不再出现“附近”，当前顺序为关注、拼单、拼车、线下组队、线上开黑。
- 关注筛选已从右侧打开，四个优先级选项完整显示且默认选中拼单优先；抽屉未遮挡底部应用按钮。
- 社区列表标题已改为“可参与活动”，列表头只保留当前排序摘要按钮，顶部搜索区仍保留统一筛选图标入口。
- 拼车筛选抽屉从右侧打开，包含上车地点优先、出行时间优先、综合优先，以及指定上车地点、目的地、出行时间输入。
- 商城筛选抽屉从右侧打开，包含商品分类、价格区间、综合/销量/价格/距离排序，以及全新、二手、有货、包邮状态。
